{
  "name": "lambda-layer-deploy",
  "version": "0.1.0",
  "description": "The deploy mechanism of lambda layer",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^1.36.3"
  },
  "devDependencies": {}
}
